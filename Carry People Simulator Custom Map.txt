--Services
local tweenservice = game:GetService("TweenService")

local player = game:GetService("Players").LocalPlayer
player.Character.HumanoidRootPart.CFrame = CFrame.new(0,10010,0)
player.Character.HumanoidRootPart.Anchored = true

pcall(function()
    if workspace.CurrentCamera.CustomMap then
        workspace.CurrentCamera.CustomMap:Destroy()
    end
end)

wait()

function build(part,buildtime,sizex,sizey,sizez)
    local tweena = tweenservice:Create(part, TweenInfo.new(buildtime), {Size = Vector3.new(sizex,sizey,sizez)})
    tweena:Play()
    tweena.Completed:Wait()
end

local baseplate = Instance.new("Part")
baseplate.Name = "CustomMap"
baseplate.Parent = workspace.CurrentCamera
baseplate.CFrame = CFrame.new(0,10000,0)
baseplate.Size = Vector3.new(0,7,0)
baseplate.Anchored = true

local buildings = Instance.new("Folder")
buildings.Name = "Buildings"
buildings.Parent = baseplate

local objects = Instance.new("Folder")
objects.Name = "Objects"
objects.Parent = baseplate

build(baseplate,0.7,65,7,65)

player.Character.HumanoidRootPart.Anchored = false

local building1 = Instance.new("Part")
building1.Name = "Part"
building1.Parent = buildings
building1.CFrame = CFrame.new(0,10010,25)
building1.Size = Vector3.new(0,7,0)
building1.Anchored = true

build(building1,0.7,7,25,7)

local building2 = Instance.new("Part")
building2.Name = "Part"
building2.Parent = buildings
building2.CFrame = CFrame.new(0,10010,-25)
building2.Size = Vector3.new(0,7,0)
building2.Anchored = true

build(building2,0.7,7,25,7)

local building3 = Instance.new("Part")
building3.Name = "Part"
building3.Parent = buildings
building3.CFrame = CFrame.new(25,10010,0)
building3.Size = Vector3.new(0,7,0)
building3.Anchored = true

build(building3,0.7,7,25,7)

local building4 = Instance.new("Part")
building4.Name = "Part"
building4.Parent = buildings
building4.CFrame = CFrame.new(-25,10010,0)
building4.Size = Vector3.new(0,7,0)
building4.Anchored = true

build(building4,0.7,7,25,7)

local object1 = Instance.new("Part")
object1.Name = "Part"
object1.Parent = buildings
object1.CFrame = CFrame.new(-17.5,10003.5,0)
object1.Size = Vector3.new(0,0.3,0)
object1.Anchored = true
object1.Color = Color3.fromRGB(95, 218, 255)
object1.Material = Enum.Material.SmoothPlastic

object1.Touched:Connect(function(hit)
    if hit.Parent:FindFirstChild("Humanoid") then
        local bv = Instance.new("BodyVelocity")
        bv.Parent = hit.Parent:FindFirstChild("HumanoidRootPart")
        bv.MaxForce = Vector3.new(math.huge,math.huge,math.huge)
        bv.Velocity = Vector3.new(0,125,0)
        wait(0.025)
        bv:Destroy()
    end
end)

build(object1,0.7,4,0.3,4)

local building5 = Instance.new("Part")
building5.Name = "Part"
building5.Parent = buildings
building5.CFrame = CFrame.new(-15,10020,15)
building5.Size = Vector3.new(0,7,0)
building5.Anchored = true

build(building5,0.7,7,3,7)

local building6 = Instance.new("Part")
building6.Name = "Part"
building6.Parent = buildings
building6.CFrame = CFrame.new(15,10020,-15)
building6.Size = Vector3.new(0,7,0)
building6.Anchored = true

build(building6,0.7,7,3,7)
